# Flagship mobile clients

This directory contains the iOS and Android scaffolds. Both ports
target the same authoritative backend contract: **the daemon's
`/api/screens/*` BFF endpoints** (see
`packages/server-daemon/src/screens/screensHttp.ts` and `types.ts`).
Mirror models live in:

- `ios/Sources/FlagshipAPI/Models/ScreensModels.swift`
- `android/app/src/main/java/com/flagship/api/ScreensModels.kt`

When the daemon's BFF contract changes, **update both mirror files in
the same commit** so Swift + Kotlin stay in lockstep with the TypeScript
source-of-truth.

## What "/api/screens/*" is

The webapp at `apps/web/public/webapp/` is the reference client and the
first surface to ship. Each screen calls exactly one endpoint shaped for
that screen (BFF discipline). When iOS / Android implementations come
online, every native screen reuses the same endpoint URL + JSON
contract — no new server-side work should be needed for the port.

The 20 endpoints (P1.1 – P1.20 in the cycle plan) cover:

| ID | Endpoint | View it powers |
|---|---|---|
| P1.1 | GET /api/screens/server-detail | Server-detail card |
| P1.2 | GET /api/screens/apps-list | Apps list |
| P1.3 | GET /api/screens/app-detail/:appId | App detail page |
| P1.4 | GET /api/screens/marketplace-browse | Marketplace |
| P1.5 | POST /api/screens/vibe-code/start | Vibe-code launcher |
| P1.6 | WS  /api/screens/vibe-code/:id/stream | Vibe-code live stream (stubbed; poll P1.7) |
| P1.7 | GET /api/screens/vibe-code/:id | Vibe-code status / replay |
| P1.8 | GET /api/screens/unlock-approvals/pending | Pending unlock requests |
| P1.9 | POST /api/screens/unlock-approvals/:requestId/approve | Approve unlock |
| P1.10 | GET /api/screens/browser-tabs/list/:appId | Browser tabs list |
| P1.11 | WS  /api/screens/browser-tabs/:tabId/stream | Framebuffer stream (stubbed; poll P1.10) |
| P1.12 | GET /api/screens/paired-sessions/list | Paired sessions |
| P1.13 | DELETE /api/screens/paired-sessions/:tokenPrefix | Revoke session |
| P1.14 | POST /api/screens/orders/send | Generic phone-order send |
| P1.15 | SSE /api/screens/install-events/:serial | Install progress (stubbed; poll .com) |
| P1.16 | GET /api/screens/tier-status | Tier dashboard |
| P1.17 | GET /api/screens/url-controller/owned | Owned URLs |
| P1.18 | POST /api/screens/url-controller/claim | Claim a URL |
| P1.19 | POST /api/screens/app-backup/start | Start an app backup |
| P1.20 | GET /api/screens/app-backup/:backupId | Fetch backup bytes |

## Authentication

All `/api/screens/*` calls are paired-session gated. The mobile client
holds a session token (32-byte hex, generated by the phone) registered
with the pod via the `add-paired-session` PhoneOrder. Send the token on
every request as the `x-flagship-session` header. The webapp's
`lib/api.js` is a faithful reference for the wire format.

## What's NOT in this README

- Native UI implementation: deferred (P5.5 / P5.6 in the cycle plan).
  The webapp is the v1 alpha surface; native ports follow once the
  contract is battle-tested in production.
- Real HTTP / WebSocket client + Secure Enclave / StrongBox key wrapping:
  deferred. The `Keystore.swift` / `Keystore.kt` scaffolds exist for the
  identity layer; the network layer needs a pass when the port begins.

## Updating the contract

1. Change the daemon's `screensHttp.ts` + `types.ts`.
2. Update integration tests in
   `packages/server-daemon/tests/screens/`.
3. Mirror the type change into:
   - `ios/Sources/FlagshipAPI/Models/ScreensModels.swift`
   - `android/app/src/main/java/com/flagship/api/ScreensModels.kt`
4. Update the table above if a new endpoint was added.

The webapp's `lib/api.js` doesn't have hardcoded model types
(everything's `JSON.parse(...)` into untyped JS), so it floats with
schema changes — but the mobile mirrors are typed and **must** be
updated.

## iOS-driven contract extensions

A handful of endpoints were added ahead of daemon-side work to power
specific iOS surfaces; both mobile mirrors have them already:

| ID | Endpoint | Surface |
|---|---|---|
| P1.21 | GET /api/screens/server-metrics/:podId | ServerDetail monitoring panel (CPU/mem/disk/I/O) |
| P1.22 | POST /api/screens/url-controller/verify | AppDetail custom-domain verify flow |
| P1.23 | GET /api/screens/post-recovery/status | Post-recovery report (J.3/J.4 reattach snapshot) |

When the daemon implements them, no client change should be required
— just `types.ts` in lockstep.

## Kotlin client status (2026-05-13)

Package is `com.flagshipserver.app` (matches the iOS bundle id
`com.flagshipserver.app`). Build: `./gradlew :app:assembleDebug` /
`:app:assembleRelease` (R8 + signing wired). Tests:
`./gradlew :app:test`.

Mirror is current with the Swift side and shipped end-to-end:

- **Client surface** — `ScreensModels.kt` covers P1.1–P1.23.
  `ScreensClient.kt` is the async + Flow-based interface (Kotlin
  Flow ≡ Swift AsyncStream for streaming endpoints).
  `LiveScreensClient.kt` is the OkHttp-backed live impl
  (SSE for install-events, WebSocket for vibe-code).
  `MockScreensClient.kt` provides realistic in-memory fixtures.
- **Pre-pairing surface** — `FlagshipServerClient.kt` declares the
  Worker endpoints; `LiveFlagshipServerClient` posts canonical-bytes
  envelopes; `MockFlagshipServerClient` simulates the same shape.
- **QR relay** — `QrSession.kt` does X25519 ECDH + HKDF →
  (kEnc, matchCode) and AES-256-GCM seals the install-blob bundle.
  `QrRelayClient.kt` (Mock + Live OkHttp WebSocket impl).
- **AppState** — StateFlow surfaces, currentPodId / leaderPodId
  semantics, SlugUtil. Mirrors iOS @Observable AppState.
- **Push** — `push/PushRegistrar` signs canonical bytes with the
  IRK and POSTs to /api/push/register; FCM service forwards taps
  through the DeepLinker.
- **Recovery** — `keystore/Recovery` (wrap/unwrap) +
  `PasskeyRecoveryManager` (CredentialManager + PRF extension) +
  `BlockStoreUmkStore` (Google Block Store envelope).
- **Live charts** — Vico (`com.patrykandpatrick.vico`) renders CPU/
  mem/disk/net time series on ServerDetail.
- **Hardening** — BiometricPrompt-gated IRK derivation,
  EncryptedSharedPreferences for session token + UMK seed,
  CertificatePinner for flagshipserver.com, R8 + proguard rules.

Test layout under `app/src/test/java/com/flagshipserver/app/`:
- pure JVM: Mock clients, QrSession crypto, ToastCenter, AppState,
  InstallBlob canonical-bytes, Recovery wrap/unwrap, HomeViewModel.
- Robolectric (no emulator): QrRelay.parseQrUrl, Base64URL.decode,
  DeepLink.parse(Uri), Compose UI tests for HomeScreen.
- MockWebServer: LiveScreensClient, LiveFlagshipServerClient.

Run `./gradlew :app:test` from Android Studio or the CLI; 86 tests
green at the last refresh.
