<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- The Burner shells out to /usr/local/bin/node to run the CLI.
         Hardened runtime would otherwise block that as a security
         feature; this entitlement re-allows it. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Read removable volumes so we can enumerate USB targets. -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Run other apps (the bundled Node CLI). -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
</dict>
</plist>
